# T-405 Task Brief - Rule manifests + fixtures + stub predicates

## Module

- ID: `adapter.catalogue` rule catalogues + `engine.validation.predicates`
- Phase: 4
- Model tier: Sonnet, with scientific review required before predicates become real
- Estimated context budget: <= 40 k tokens per rule category

## Inputs

- `CODING_AGENDA.md` § 2.4.5
- `REQUIREMENTS.md` § 4.1-4.5
- `Cloning_and_Expression_Vector_Design_Knowledge_Base_v2_0.md` §§ 9, 12, 17, 18
- `task_artefacts/T-401/post_execution.md`

## Deliverables

- Rule manifests in `catalogues/rules/{MR,WR,SR,BR,MS}.yaml`.
- Rule schemas in `schemas/rules_{MR,WR,SR,BR,MS}.schema.json`.
- Stub predicate registry in `src/engine/validation/predicates/`.
- Triggering and passing fixtures for every rule in `tests/fixtures/rules/<category>/`.
- Active informational gates:
  - `tools/ci_gates/rule_fixture_coverage_check.py`
  - `tools/ci_gates/implementation_status_consistency_check.py`

## Acceptance

```yaml
task_id: T-405
required_checks:
  - name: focused_rule_catalogue_tests
    command: "python -m uv run --no-editable pytest tests/adapter/catalogue tests/ci_gates/test_t401_catalogue_gates.py tests/ci_gates/test_t405_rule_gates.py --cov=adapter.catalogue --cov=tools.ci_gates.stale_catalogue_check --cov=tools.ci_gates.source_grade_citation_check --cov=tools.ci_gates.rule_fixture_coverage_check --cov=tools.ci_gates.implementation_status_consistency_check --cov=engine.validation --cov-report=term-missing --cov-fail-under=85"
    expected: "30 passed; coverage >= 85%"
  - name: package_refresh
    command: "python -m uv sync --frozen --no-editable --group dev --extra io --reinstall-package cloning-expression-vector-design"
    expected: "package rebuilt and reinstalled"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "309 passed, 2 skipped"
  - name: rule_gates
    command: "python -m uv run --no-editable pytest tests/ci_gates/test_t405_rule_gates.py"
    expected: "rule fixture and implementation-status gates pass in enforce mode"
```

## Acceptance Detail

- MR, WR, SR, BR, and MS manifests exist with required T-405 metadata and expected rule counts.
- Every manifest predicate name resolves to a stub returning `Severity.INFO`.
- Every rule declares unique triggering and passing fixture paths that resolve on disk.
- `rule-fixture-coverage-check` and `implementation-status-consistency-check` are active as informational gates.

## Hand-off

T-406 can consume the rule manifest structure and gate conventions while adding vendor, screening, policy, export, and advisory catalogues. Phase 5/6 predicate tasks replace these stubs with real biological checks and advance implementation-status enforcement.
