# T-304 Task Brief — `domain.security` (SecurityRole + Principal + AuthorisationProfile; v1.3 — profile signature per B3-04; `DeveloperBootstrapPrincipal` per H3-10)

## Scope

Implement the T-304 security primitives from `CODING_AGENDA.md` § 2.3.4:

- `SecurityRole`, `OperationalRole`, and one-way role inheritance;
- principal hierarchy including `DeveloperBootstrapPrincipal`;
- `CoveredBiologicalScope` and `DualControlFlags`;
- unsigned authorisation profile drafts and signed `AuthorisationProfile`;
- `ProfileSignature`, `UserDeclaration`, and `AuthorisationDecision`.

Production signer/verifier protocols and adapters remain scheduled for T-314a/T-314b.

## Acceptance

```yaml
task_id: T-304
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/security --cov=domain.security --cov-fail-under=85"
    covers:
      - "role inheritance semantics"
      - "DeveloperBootstrapPrincipal bootstrap authority"
      - "signed AuthorisationProfile content-hash invariants"
      - "CoveredBiologicalScope and UserDeclaration validation"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green after replacing the T-203 security stub"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "domain import boundaries remain intact"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-304/post_execution.md`.
