# T-312a Task Brief — `AuditKeyProvider` Protocol + `TestAuditKeyProvider` (v1.4 — Protocol surface revised per H4-10: no raw `current_key()` method)

## Scope

Implement the early audit-key split from `CODING_AGENDA.md` § 2.3.8:

- define `domain.ports.audit_key.AuditKeyProvider`;
- define typed `KeyVersionId` and `MacBytes`;
- guarantee the Protocol exposes HMAC operations only, never raw key bytes;
- provide a deterministic in-memory `TestAuditKeyProvider` for downstream audit-chain tests;
- add reusable contract tests for any future conforming implementation.

## Acceptance

```yaml
task_id: T-312a
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/ports/test_audit_key_provider_contract.py tests/fakes/security/audit_key/test_audit_key_provider.py tests/ports/test_port_inventory.py --cov=domain.ports --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "AuditKeyProvider exposes mac/verify/verify_with_archived/rotate/current_key_version"
      - "No current_key/raw-key method is exposed"
      - "TestAuditKeyProvider verifies HMACs, rejects tampering, rotates deterministically, and verifies archived versions"
      - "Port inventory still matches the 50 canonical ports"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-312a/post_execution.md`.
