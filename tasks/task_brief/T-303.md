# T-303 Task Brief — `domain.types` core entities

## Scope

Implement the T-303 core entity split from `CODING_AGENDA.md` § 2.3.3:

- T-303a: `Part`, `Host`, `HostContext`, `Module`, `Construct`;
- T-303b: `Library`, deterministic `domain.library.expand`, assembly methods, and typed
  assembly plan summaries;
- T-303c: `ValidationRule` and `HostCompatibilityConstraints`.

Later T-306/T-307 namespaces remain out of scope.

## Acceptance

```yaml
task_id: T-303
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/types --cov=domain.types --cov=domain.library --cov-fail-under=85"
    covers:
      - "core entity construction invariants"
      - "Construct.feature_table == derive_feature_table(graph)"
      - "deterministic finite library expansion"
      - "typed assembly plan chemistry invariants"
      - "ValidationRule gate-policy invariants"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green after replacing the T-203 types/library stubs"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "domain import boundaries remain intact"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-303/post_execution.md`.
