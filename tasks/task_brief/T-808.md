# T-808 Task Brief — `app.plugin_governance` (v1.1 — new, per B-02)

## Scope

Implement Phase 8a plugin-governance verification.

- Load signed plugin manifests from `catalogues/plugin_manifests/*.yaml`.
- Verify Ed25519 signatures against the institutional plugin trust keyring.
- Verify each loaded artefact hash matches the manifest declaration.
- Enforce declared file/network permissions through a deterministic sandbox grant.
- Emit `PluginManifestApproved` and `PluginManifestRejected` governance events.
- Advance the `plugin-manifest-signature` CI gate to enforced mode.

## Acceptance

```yaml
task_id: T-808
implemented_files:
  - src/app/plugin_governance.py
  - tools/ci_gates/plugin_manifest_signature_check.py
  - tests/app/test_plugin_governance_t808.py
  - catalogues/plugin_manifests/plugin_manifest_seed.yaml
  - schemas/plugin_manifests.schema.json
required_checks:
  - python -m uv run --no-editable ruff check src/app/plugin_governance.py tools/ci_gates/plugin_manifest_signature_check.py tests/app/test_plugin_governance_t808.py tests/ci_gates/test_t204_gates.py
  - python -m uv run --no-editable mypy src/app/plugin_governance.py tools/ci_gates/plugin_manifest_signature_check.py tests/app/test_plugin_governance_t808.py tests/ci_gates/test_t204_gates.py
  - python -m uv run --no-editable pytest tests/app/test_plugin_governance_t808.py tests/ci_gates/test_t204_gates.py -q
  - python -m uv run --no-editable python -m tools.ci_gates.plugin_manifest_signature_check --enforce
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
verification_result:
  focused: 10 passed
  plugin_manifest_signature_gate: passed
  full: 476 passed, 2 skipped
```

## Notes

Refined and completed during T-808 execution.
