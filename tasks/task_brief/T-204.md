# T-204 Task Brief — CI gate skeletons with lifecycle states (v1.4 — manifest authority fixed per B4-07; expanded security gates per H4-02; initial-brief generator per H4-03)

## Scope

Implemented in this pass: lifecycle-aware CI gate skeletons, manifest generators, task-count reporter, seed task briefs for all active cards, and informational workflow wiring.

## Acceptance

```yaml
task_id: T-204
required_checks:
  - tests/ci_gates/test_t204_gates.py
  - python -m uv run --no-editable python -m tools.ci_gates.module_coverage_check --informational
  - python -m uv run --no-editable python -m tools.ci_gates.task_acceptance_completeness_check --informational
  - python -m uv run --no-editable python -m tools.ci_gates.gate_lifecycle_check --informational
```

## Notes

Generated by `tools/initial_task_brief_generator.py`.
