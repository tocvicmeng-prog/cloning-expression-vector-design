# T-316b Task Brief — Signed SQLite SOP-template store and bootstrap migration

## Module

- IDs: `adapter.persistence`, `app.admin_action_handler`
- Phase: 4
- Model tier: Opus
- Estimated context budget: ≤ 55 k tokens

## Inputs

- `CODING_AGENDA.md` § 2.4.8
- `tasks/task_brief/T-316a.md`
- `tasks/task_brief/T-316c.md`
- `task_artefacts/T-316c/post_execution.md`

## Deliverables

- SQLite-backed `SopTemplateReadPort`, `SopTemplateAdminWritePort`, and `SopTemplateBootstrapPort` implementation.
- Signature verification on every read; unsigned or tampered persisted templates fail.
- Admin-handler SOP-template mint/modify/revoke verbs that sign before commit and emit audit/governance records.
- Bootstrap migration from `catalogues/sop_templates/*.yaml`, using the production T-316c signer and verifier.
- Idempotent bootstrap: unchanged YAML produces no new rows or governance events on repeat runs.

## Acceptance

```yaml
task_id: T-316b
phase: 4
owning_modules:
  - adapter.persistence
  - app.admin_action_handler
required_checks:
  - "python -m uv run --no-editable pytest tests\\adapter\\persistence\\test_sqlite_sop_template_store.py tests\\app\\sop_template\\test_admin_handler_sop_templates.py tests\\security\\sop_template_signing tests\\domain\\events\\test_events.py --cov=adapter.persistence.sqlite_sop_template_store --cov=app.admin_action_handler --cov=adapter.security.sop_template_signing --cov=domain.events --cov-report=term-missing --cov-fail-under=85"
gates_activated: []
gates_advanced_to: []
acceptance:
  - id: A1
    description: "SQLite SOP-template store writes, reads, lists, and revokes signed templates."
    test: "tests/adapter/persistence/test_sqlite_sop_template_store.py"
    blocks_verification: true
  - id: A2
    description: "Tampered persisted template JSON fails on read via SOP-template signature verification."
    test: "tests/adapter/persistence/test_sqlite_sop_template_store.py::test_sqlite_sop_template_store_rejects_tampered_persisted_template"
    blocks_verification: true
  - id: A3
    description: "Bootstrap reads catalogue YAML, signs templates, emits governance events, and is idempotent."
    test: "tests/adapter/persistence/test_sqlite_sop_template_store.py::test_sqlite_sop_template_bootstrap_reads_catalogue_and_is_idempotent"
    blocks_verification: true
  - id: A4
    description: "Admin handler signs, stores, audits, and emits governance events for SOP-template mint/modify/revoke."
    test: "tests/app/sop_template/test_admin_handler_sop_templates.py"
    blocks_verification: true
  - id: A5
    description: "User and reviewer principals cannot write SOP templates."
    test: "tests/app/sop_template/test_admin_handler_sop_templates.py::test_user_and_reviewer_cannot_write_sop_templates"
    blocks_verification: true
ci_gates_touched: []
```

## Downstream

- T-803 consumes signed templates through `SopTemplateReadPort`.
- T-1103b must expose SOP-template admin-write verbs only through the admin-service IPC boundary.
