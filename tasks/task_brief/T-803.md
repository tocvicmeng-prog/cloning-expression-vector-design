# T-803 Task Brief — `engine.sop_protocol` (gated; v1.3 — SOP-template admin writes moved to T-316b per B3-05/B4-05; consumes read port only)

## Scope

Implement `engine.sop_protocol` as the gated SOP-linked protocol generator. The generator consumes only `SopTemplateReadPort`, refuses unsigned or tampered templates, requires an observed `OperationalProtocolAuthorised` design event before reading the template, emits `SopLinkedProtocol` values backed by `domain.types.sop_protected`, and provides deterministic JSON / Markdown / PDF renderers without embedded timestamps.

## Acceptance

```yaml
task_id: T-803
required_checks:
  - python -m uv run --no-editable ruff check src\engine\sop_protocol tests\engine\sop_protocol tools\ci_gates\sop_after_gates_check.py tests\ci_gates\test_t204_gates.py
  - python -m uv run --no-editable mypy src\engine\sop_protocol tests\engine\sop_protocol tools\ci_gates\sop_after_gates_check.py tests\ci_gates\test_t204_gates.py
  - python -m uv run --no-editable pytest tests\engine\sop_protocol tests\ci_gates\test_t204_gates.py -q
  - python -m uv run --no-editable python -m tools.ci_gates.sop_after_gates_check --enforce
  - python -m uv run --no-editable lint-imports --config .importlinter
  - python -m uv run --no-editable python tools\ci\run_pytest.py -m "not slow"
```

## Notes

Completed locally on 2026-05-14. Post-execution notes live in `task_artefacts/T-803/post_execution.md`.
