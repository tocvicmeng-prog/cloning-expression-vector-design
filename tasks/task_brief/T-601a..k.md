# T-601a..k Task Brief — Biology Adapter Implementations

## Module

- ID: `adapter.biology`
- Phase: 6
- Model tier: Sonnet
- Estimated context budget: <= 30 k tokens per adapter.

## Inputs

- `CODING_AGENDA.md` § 2.6.1.
- `ARCHITECTURE.md` § 4.5 biology ports.
- `domain.ports` contracts for `RnaFolder`, `SplicePredictor`, `SignalPeptidePredictor`, `TIRPredictor`, `KozakScorer`, and `CodonAlgorithm`.

## Deliverables

- `src/adapter/biology/` package replacing the T-203 placeholder module.
- Deterministic local adapters for RNA folding, splice motif prediction, signal peptide prediction, RBS/TIR scoring, Kozak scoring, and CAI / MinMax / CHARMING / avoid-only codon algorithms.
- Adapter manifests with measured local latency fields and deterministic/external-service flags.
- Fixture-driven deterministic fakes in `tests/fakes/biology/`.
- Calibration-drift policy files in `tests/calibration/biology/<adapter>/policy.yaml`.

## Acceptance

```yaml
task_id: T-601a..k
phase: 6
owning_modules:
  - adapter.biology
required_checks:
  - "python -m uv run --no-editable pytest tests\\adapter\\biology tests\\fakes\\biology --cov=adapter.biology --cov=tests.fakes.biology --cov-report=term-missing --cov-fail-under=85"
  - "python -m uv run --no-editable python tools\\ci\\run_pytest.py -m \"not slow\""
acceptance:
  - id: A1
    description: "All T-601 adapter manifests are exported with deterministic latency metadata."
    test: "tests/adapter/biology/test_biology_adapters.py::test_package_exports_all_t601_adapter_manifests"
    blocks_verification: true
  - id: A2
    description: "Biology adapter methods return deterministic metric payloads."
    test: "tests/adapter/biology/test_biology_adapters.py"
    blocks_verification: true
  - id: A3
    description: "Fixture-driven fakes match production adapter payloads for curated inputs."
    test: "tests/adapter/biology/test_biology_adapters.py::test_fixture_fakes_are_deterministic_and_fixture_driven"
    blocks_verification: true
  - id: A4
    description: "Every adapter has a calibration drift policy file."
    test: "tests/adapter/biology/test_biology_adapters.py::test_calibration_policy_files_exist_for_every_adapter"
    blocks_verification: true
ci_gates_touched: []
```

## Notes

The initial T-601 implementation is deliberately local and deterministic. Heavy production back-ends remain optional behind extras or future service adapters, which keeps the no-editable Windows/OneDrive development path fast and reliable while preserving the port shapes required by T-602/T-603.
