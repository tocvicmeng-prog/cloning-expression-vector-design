# T-314a Task Brief — `AuthorisationProfileSigner` + `AuthorisationProfileVerifier` + `DecisionRecordSigner` + `DecisionRecordVerifier` Protocols + fakes (v1.4 — early-half split per B4-02 / B4-09; scheduled before T-308 / T-310 / T-311 so they can depend on it)

## Scope

Implement the early signing split from `CODING_AGENDA.md` § 2.3.10:

- define `AuthorisationProfileSigner` / `AuthorisationProfileVerifier` Protocols;
- define `DecisionRecordSigner` / `DecisionRecordVerifier` Protocols;
- define `SignedDecisionRecord`;
- define the signing verification result/error taxonomy;
- provide deterministic fake signers/verifiers for T-310/T-311 and later admin/auth workflows.

## Acceptance

```yaml
task_id: T-314a
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/ports/test_profile_signing_contract.py tests/domain/ports/test_decision_record_signing_contract.py tests/fakes/security/profile_signing/test_fake_signers.py tests/ports/test_port_inventory.py --cov=domain.ports --cov=domain.types.signing_errors --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "profile sign-then-verify success"
      - "decision-record sign-then-verify success"
      - "tamper, unknown-key, revoked-key, and principal-mismatch failures"
      - "port inventory still matches the 50 canonical ports"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-314a/post_execution.md`.
