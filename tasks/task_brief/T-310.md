# T-310 Task Brief — `adapter.persistence` implementations

## Scope

Implement the Phase-3 persistence adapters from `CODING_AGENDA.md` § 2.3.14:

- SQLite project/session store with WAL + `BEGIN IMMEDIATE`;
- append-only JSONL event logs for design/governance/export streams;
- SQLite audit-log read interface with HMAC-chain verification through `AuditKeyProvider`;
- read-only SQLite authorisation profile store with `AuthorisationProfileVerifier` on every load;
- filesystem snapshot store keyed by derivation-environment hash.

## Acceptance

```yaml
task_id: T-310
required_checks:
  - command: "python -m uv run --no-editable pytest tests/adapter/persistence tests/engine/session --cov=adapter.persistence --cov=engine.session --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "SQLite WAL project/session persistence"
      - "append-only JSONL logs and replay determinism"
      - "trailing partial-line crash recovery"
      - "audit HMAC-chain verification, tamper detection, and missing-key refusal"
      - "read-only authorisation store with verifier-on-load"
      - "filesystem snapshot retention by derivation hash"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-310/post_execution.md`.
