# T-903 Task Brief — `app.export_orchestrator` + redaction (v1.2 — moved to Phase 9b per B2-02; renderers per M2-04)

## Scope

Implemented in `src/app/export_orchestrator/` and `src/engine/export_gate.py`.

The T-903 slice builds deterministic final export ZIPs after screening,
authorisation, and SOP rendering. Export profiles apply redaction at
serialisation time, manifests name every artefact with content hashes, and the
final `BlockExport` predicate composes prior screening predicates before
requiring authorisation and SOP evidence.

## Acceptance

```yaml
task_id: T-903
required_checks:
  - PYTHONPATH=src python -m ruff check src/app/export_orchestrator src/engine/export_gate.py tests/app/test_export_orchestrator_t903.py tests/engine/test_export_gate_t903.py
  - PYTHONPATH=src python -m pytest tests/app/test_export_orchestrator_t903.py tests/engine/test_export_gate_t903.py
  - PYTHONPATH=src;. python tools/ci_gates/no_passive_advisory_bypass_check.py
  - PYTHONPATH=src python tools/agenda_consistency_check.py
```

## Notes

Complete locally on 2026-05-14. Focused T-903 slice: 6 tests passed. The project
interpreter is currently used with `PYTHONPATH=src` because this shell does not
have `uv` on PATH and the existing `.venv` site-package copy was stale after
converting `app.export_orchestrator` from a module to a package.
