# T-316a Task Brief — `SopTemplate` split-port Protocols + `SopTemplateSigner` / `SopTemplateVerifier` Protocols + value objects (v1.4 — early-half split per B4-05; Phase 3; production SQLite store + bootstrap moved to T-316b in Phase 4)

## Scope

Implement the early SOP-template split from `CODING_AGENDA.md` § 2.3.11:

- define `SopTemplateReadPort`, `SopTemplateAdminWritePort`, and `SopTemplateBootstrapPort`;
- define `SopTemplateSigner` and `SopTemplateVerifier`;
- define SOP-template value objects and signature binding;
- provide deterministic fake SOP-template signer/verifier for T-316b and T-803 tests.

## Acceptance

```yaml
task_id: T-316a
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/types/test_sop_template.py tests/domain/ports/test_sop_template_contract.py tests/fakes/sop_template/test_fake_signer.py tests/ports/test_port_inventory.py --cov=domain.ports --cov=domain.types.sop_template --cov=domain.types.signing_errors --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "SOP-template value objects and content-hash/signature binding"
      - "SOP-template signer/verifier Protocols"
      - "tamper, unsigned, unknown-key, and revoked-key failures"
      - "port inventory still matches the 50 canonical ports"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-316a/post_execution.md`.
