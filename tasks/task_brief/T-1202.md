# T-1202 Task Brief — React + TypeScript SPA

## Scope

Complete locally. Implement the Phase 12 React + TypeScript SPA in a separate `ui/` package and replace the `interface.ui` placeholder with metadata helpers that locate the UI package from Python.

## Acceptance

```yaml
task_id: T-1202
required_checks:
  - npm test
  - npm run build
  - python -m pytest tests/interface/ui/test_ui_t1202.py -q
  - python -m ruff check .
  - python -m mypy src tests
  - python -m pytest -m "not slow" -q
```

## Notes

- Added `ui/` Vite/React/TypeScript package with decision tree wizard, vector map, validation panel, advisory acknowledgement dialog, admin console, audit-log viewer, and design diff.
- Advisory acknowledgement exposes only acknowledge / decline / escalate paths before the commit action and has no dismiss/close button.
- Full local verification passed on 2026-05-14 with 591 Python tests passed and 2 skipped.
