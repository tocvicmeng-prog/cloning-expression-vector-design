# T-315 Task Brief — `app.review_queue` + `AuthorisationRequest` service

## Scope

Implement the Phase-3 review queue from `CODING_AGENDA.md` § 2.3.19:

- `AuthorisationRequest`, `ReviewQueueItem`, and signed `ReviewQueueItemDecision` value objects;
- typed `ReviewQueueStore` and admin-only `ReviewQueueAdminPort`;
- append-only SQLite queue where requests are immutable and decisions are separate rows;
- `ReviewQueueService` user/service submission paths for FR-AUTH-12 and FR-PROTO-SOP-09;
- admin-resolution helper for the future admin-service process, with signed decision-record payloads embedded in governance events;
- adversarial tests proving user/reviewer paths cannot self-approve and requests never auto-grant profiles.

## Acceptance

```yaml
task_id: T-315
required_checks:
  - name: focused_review_queue_tests
    command: "python -m uv run --no-editable pytest tests/app/review_queue tests/ports/test_port_inventory.py tests/domain/events/test_events.py --cov=app.review_queue --cov=app.review_queue_service --cov=adapter.persistence.sqlite_review_queue_store --cov=domain.types.review_queue --cov=domain.ports.review_queue_admin --cov-report=term-missing --cov-fail-under=85"
    expected: "21 passed; coverage >= 85%"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "279 passed, 2 skipped"
```

## Post-Execution Notes

Completed locally on 2026-05-14. T-401 is next.
