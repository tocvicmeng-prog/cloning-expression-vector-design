# T-807 Task Brief — `engine.vlp_policy` (v1.1 — new, per H-04 / B-02)

## Scope

Implement the Phase 8a VLP and viral-vector policy engine.

- Distinguish MS2 RNA-display systems, phage-derived VLPs, AAV, and lentiviral vectors as separate system classes.
- Enforce non-operational policy checks for packaging-signal declarations, capsid/helper separation, cargo-size limits, replication/infectivity boundaries, controls, and assembly-readout metadata.
- Connect MS-01..MS-07 policy findings to the existing MS rule registry while preserving the T-405 validation predicate stubs.
- Emit deterministic `VlpPolicyReport` values with stable content hashes and risk-classification trigger tags.
- Keep the package outside the gated `domain.types.sop_protected` namespace.

## Acceptance

```yaml
task_id: T-807
implemented_files:
  - src/engine/vlp_policy/__init__.py
  - src/engine/vlp_policy/policy.py
  - src/engine/vlp_policy/system_classes.py
  - tests/engine/test_vlp_policy_t807.py
required_checks:
  - python -m uv run --no-editable ruff check src/engine/vlp_policy tests/engine/test_vlp_policy_t807.py
  - python -m uv run --no-editable mypy src/engine/vlp_policy tests/engine/test_vlp_policy_t807.py
  - python -m uv run --no-editable pytest tests/engine/test_vlp_policy_t807.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
verification_result:
  focused: 7 passed
  full: 470 passed, 2 skipped
```

## Notes

Refined and completed during T-807 execution.
