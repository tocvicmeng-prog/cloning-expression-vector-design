# T-311 Task Brief — `app.admin_action_handler`

## Scope

Implement the Phase-3 admin authorisation write surface from `CODING_AGENDA.md` § 2.3.15:

- split `domain.ports.authorisation` read/admin/bootstrap Protocols;
- SQLite authorisation write adapter for admin-service use;
- `AdminActionHandler` mint/modify/revoke/list/audit/bootstrap operations;
- profile signing through `AuthorisationProfileSigner`;
- audit appends through `AdminAuditAppendPort`;
- governance events with embedded signed profile payloads;
- adversarial denial of User/Reviewer/ordinary Developer write attempts.

## Acceptance

```yaml
task_id: T-311
required_checks:
  - command: "python -m uv run --no-editable pytest tests/app/admin_action_handler tests/adapter/persistence/test_sqlite_authorisation_store_read.py tests/fakes/security/profile_signing/test_fake_signers.py --cov=app.admin_action_handler --cov=adapter.persistence.sqlite_authorisation_store_write --cov=adapter.persistence.sqlite_authorisation_store_read --cov=domain.ports.authorisation --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "signed profile mint/modify/revoke round trips"
      - "admin audit append path"
      - "governance events with embedded profile payloads"
      - "User/Reviewer denial"
      - "DeveloperBootstrap one-shot bootstrap"
      - "ordinary Developer denial after bootstrap"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-311/post_execution.md`.
