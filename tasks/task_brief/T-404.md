# T-404 Task Brief - Enzymes catalogue + buffer-compatibility matrix

## Scope

Populate `catalogues/enzymes.yaml` and add `catalogues/enzyme_buffer_compat.yaml` from `CODING_AGENDA.md` section 2.4.4 and KB v2.0 section 7.

The catalogue must include:

- at least 40 common Type-II restriction enzymes;
- Type-IIS enzymes used for Golden Gate and related kits;
- toolkit enzymes for ligation, phosphorylation, polymerase, exonuclease, template removal, phosphatase, and USER workflows;
- proteases used for fusion-tag removal;
- a buffer-compatibility matrix that covers every enzyme id in the enzyme catalogue.

## Acceptance

```yaml
task_id: T-404
required_checks:
  - name: focused_enzymes_catalogue_tests
    command: "python -m uv run --no-editable pytest tests/adapter/catalogue tests/ci_gates/test_t401_catalogue_gates.py --cov=adapter.catalogue --cov=tools.ci_gates.stale_catalogue_check --cov=tools.ci_gates.source_grade_citation_check --cov-report=term-missing --cov-fail-under=85"
    expected: "16 passed; coverage >= 85%"
  - name: package_refresh
    command: "python -m uv sync --frozen --no-editable --group dev --extra io --reinstall-package cloning-expression-vector-design"
    expected: "package rebuilt and reinstalled"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "295 passed, 2 skipped"
```

## Post-Execution Notes

Completed locally on 2026-05-14. T-405 is next.
