# T-306 Task Brief — Operational namespace + non-operational type packages (v1.1 — B-01 fix)

## Scope

Implement the T-306 type namespace split from `CODING_AGENDA.md` § 2.3.6:

- non-operational `domain.types.design_plan`;
- non-operational `domain.types.controls`;
- non-operational `domain.types.risk_advisory`;
- governance value objects in `domain.types.governance`;
- operational `domain.types.sop_protected` with `ProtocolStep`, `ProtocolDAG`, and
  `SopLinkedProtocol`.

The design-plan namespace must not import or contain SOP-protected operational values.

## Acceptance

```yaml
task_id: T-306
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/types/test_t306 --cov=domain.types.design_plan --cov=domain.types.controls --cov=domain.types.risk_advisory --cov=domain.types.governance --cov=domain.types.sop_protected --cov-fail-under=85"
    covers:
      - "DesignRealisationPlan runtime guard rejects sop_protected values"
      - "ProtocolDAG canonical ordering and cycle detection"
      - "RiskAdvisoryReport required acknowledgement set"
      - "RiskAdvisoryAcknowledgement justification rule"
      - "DecisionRecord signature invariant"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "non-SOP packages cannot import domain.types.sop_protected"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-306/post_execution.md`.
