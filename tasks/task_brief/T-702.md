# T-702 Task Brief — `engine.overhang` (Golden Gate fidelity optimiser)

## Scope

Convert the T-203 `engine.overhang` placeholder into a pure Golden Gate overhang fidelity package.

Implemented files:

- `src/engine/overhang/__init__.py`
- `src/engine/overhang/types.py`
- `src/engine/overhang/dataset.py`
- `src/engine/overhang/optimiser.py`
- `tests/engine/overhang/test_optimiser_t702.py`

## Acceptance

```yaml
task_id: T-702
required_checks:
  - python -m uv run --no-editable ruff check src/engine/overhang tests/engine/overhang/test_optimiser_t702.py
  - python -m uv run --no-editable mypy src/engine/overhang tests/engine/overhang/test_optimiser_t702.py
  - python -m uv run --no-editable pytest tests/engine/overhang/test_optimiser_t702.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
```

## Notes

Complete locally on 2026-05-14.

- Adds Potapov 2018 / Pryor 2020 labelled matrix profiles and high-fidelity benchmark fixtures.
- Scores overhang sets by per-overhang correct-ligation probability and product fidelity.
- Rejects or penalises palindrome and reverse-complement conflicts.
- Uses bounded branch-and-bound search with direct exact scoring for fixed benchmark sets and deterministic large-set handling for the 20-fragment performance path.
