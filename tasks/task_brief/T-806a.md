# T-806a Task Brief — Advisory presentation + acknowledgement surface (v1.3 — `AdvisoryWarningPresented` schema explicit per H3-11)

## Scope

Implement the Phase 8a active advisory presentation and acknowledgement surface.

- Present `RiskAdvisoryReport` values to recipients by emitting `AdvisoryWarningPresented` governance events.
- Implement explicit acknowledge, decline, and escalate actions over `RiskAdvisoryAcknowledgement` and signed `DecisionRecord` values.
- Enforce reviewer/administrator authority for binding advisory actions and require institutional approval IDs for escalations.
- Embed full acknowledgement payloads into `RiskAdvisoryAcknowledged`, `RiskAdvisoryDeclined`, and `RiskAdvisoryEscalated` governance events.
- Implement `all_required_advisories_acknowledged(report, events)` as the pure predicate consumed by Phase 8b authorisation.

## Acceptance

```yaml
task_id: T-806a
implemented_files:
  - src/app/advisory_acknowledgement.py
  - tests/app/test_advisory_acknowledgement_t806a.py
required_checks:
  - python -m uv run --no-editable ruff check src/app/advisory_acknowledgement.py tests/app/test_advisory_acknowledgement_t806a.py
  - python -m uv run --no-editable mypy src/app/advisory_acknowledgement.py tests/app/test_advisory_acknowledgement_t806a.py
  - python -m uv run --no-editable pytest tests/app/test_advisory_acknowledgement_t806a.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
verification_result:
  focused: 5 passed
  full: 463 passed, 2 skipped
```

## Notes

Refined and completed during T-806a execution.
