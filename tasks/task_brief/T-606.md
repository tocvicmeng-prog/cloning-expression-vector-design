# T-606 Task Brief — `app.design_service`

## Scope

Implement the Phase-6 design-session application service. This task owns the top-level create/open/amend/compile/replay use cases over the event-sourced `engine.session` state machine and exposes phase-local pending states for downstream services that are intentionally not implemented in Phase 6.

## Inputs

- `ARCHITECTURE.md` v1.5 § 4.2.2.
- `CODING_AGENDA.md` v1.5 § 2.6.4.
- `domain.events.design` event subclasses.
- `engine.session` state machine, replay helpers, snapshots, and gate registry.

## Deliverables

- Replace `src/app/design_service.py` placeholder with the public T-606 service.
- Add deterministic in-memory fake-backed tests for create/open/amend/compile/replay.
- Add pending-state coverage for `AwaitingScreening`, `AwaitingAuthorisation`, `AwaitingSopRender`, and `AwaitingExport`.
- Confirm T-606 does not stub or invoke screening, authorisation, SOP rendering, or export services.

## Acceptance

```yaml
task_id: T-606
required_checks:
  - python -m uv run --no-editable ruff check src/app/design_service.py tests/app/test_design_service_t606.py
  - python -m uv run --no-editable mypy src/app/design_service.py tests/app/test_design_service_t606.py
  - python -m uv run --no-editable pytest tests/app/test_design_service_t606.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
```

## Notes

`DesignService.current_pending_state(session_id)` returns a typed pending step by replaying the session stream and mapping the current `SessionState` to the T-309 gate registry. The method reports the absent downstream owner but does not instantiate or fake those downstream services.
