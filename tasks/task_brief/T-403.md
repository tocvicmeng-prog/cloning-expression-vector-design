# T-403 Task Brief - Hosts catalogue

## Scope

Populate `catalogues/hosts.yaml` from `Cloning_and_Expression_Vector_Design_Knowledge_Base_v2_0.md` section 6.

The host catalogue must include strain-level entries across:

- E. coli propagation and expression strains;
- cell-free expression systems;
- S. cerevisiae and Komagataella/Pichia yeast hosts;
- mammalian transient, stable, and stem-cell contexts;
- insect, plant, and phage/VLP production contexts.

Each entry must include chassis class, host roles, genotype or genotype caveat, supplier/source reference, supported replicons, expression features, codon-usage table id, growth-condition caveat, biosafety tier, maintenance metadata, and a graded citation resolving to KB v2.0 section 18.

## Acceptance

```yaml
task_id: T-403
required_checks:
  - name: focused_hosts_catalogue_tests
    command: "python -m uv run --no-editable pytest tests/adapter/catalogue tests/ci_gates/test_t401_catalogue_gates.py --cov=adapter.catalogue --cov=tools.ci_gates.stale_catalogue_check --cov=tools.ci_gates.source_grade_citation_check --cov-report=term-missing --cov-fail-under=85"
    expected: "13 passed; coverage >= 85%"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "292 passed, 2 skipped"
```

## Post-Execution Notes

Completed locally on 2026-05-14. T-404 is next.
