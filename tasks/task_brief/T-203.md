# T-203 Task Brief — Public API Stubs

## Scope

Create importable public API placeholder modules for the source-layer module manifest and declare the 50 canonical architecture ports as `Protocol` stubs under `domain.ports`.

## Acceptance

```yaml
task_id: T-203
required_checks:
  - tests/test_t203_module_manifest.py
  - tests/ports/test_port_inventory.py
  - python -m uv run --no-editable lint-imports --config .importlinter
  - python -m uv run --no-editable mypy src tools tests
```

## Notes

Most module files intentionally contain only `PublicApiStub` and `module_not_implemented()`. The owning downstream task replaces each placeholder with real typed public API signatures and tests.
