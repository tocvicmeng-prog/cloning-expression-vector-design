# T-307 Task Brief — `domain.types.derivation` + RFC 8785 JCS canonicalisation (v1.1 — H-06 fix)

## Scope

Implement the T-307 deterministic derivation slice from `CODING_AGENDA.md` § 2.3.7:

- convert `domain.canonicalisation` from a placeholder module into a package;
- implement RFC 8785 JSON Canonicalisation Scheme with project `$$cev:` tagged scalar extensions;
- add byte-identical golden-vector coverage for canonical JSON edge cases;
- implement `domain.types.derivation` value objects and policy enums;
- implement `DerivationEnvironmentV14.canonical_json()` and `hash()`.

## Acceptance

```yaml
task_id: T-307
required_checks:
  - command: "python -m uv run --no-editable pytest tests/canonicalisation tests/domain/types/test_derivation_environment.py --cov=domain.canonicalisation --cov=domain.types.derivation --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "RFC 8785 canonical object ordering, including UTF-16 key sorting"
      - "Decimal, datetime, enum, bytes, tuple/list, and dataclass canonicalisation"
      - "NaN, Infinity, reserved $$cev: tags, non-string keys, and naive datetimes rejected"
      - "DerivationEnvironmentV14 canonical bytes and SHA-256 hash stability"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-307/post_execution.md`.
