# T-1102 Task Brief — FastAPI HTTP server + WebSocket (v1.4 — admin routes via `AdminServiceClientPort` per B4-08)

## Scope

Implement the Phase 11 HTTP/WebSocket interface surface.

- Replace the `interface.api` placeholder with a package containing route metadata, an injectable API runtime, and a server adapter.
- Expose the same command surface as the T-1101 CLI over HTTP route metadata.
- Provide a validation WebSocket stream surface for live validation progress.
- Route profile, SOP-template, audit-key, and review-queue admin endpoints through `AdminServiceClientPort`; API code must not import the admin write-side action surface.
- Support FastAPI when the optional `api` extra is installed, while preserving a dependency-free route index and runtime tests in the base/dev environment.

## Acceptance

```yaml
task_id: T-1102
required_checks:
  - python -m ruff check src/interface/api tests/interface/api/test_api_t1102.py
  - python -m mypy src/interface/api tests/interface/api/test_api_t1102.py
  - python -m pytest tests/interface/api/test_api_t1102.py
  - python -m pytest tests/interface/api/test_api_t1102.py tests/interface/cli/test_cli_t1101.py tests/domain/ports/test_admin_service_client_contract.py tests/fakes/admin_service/test_in_memory_client.py tests/ci_gates/test_t204_gates.py
  - python -m ruff check src tests tools
  - python -m mypy src tools tests
  - python tools/agenda_consistency_check.py
  - python tools/ci/run_pytest.py -m "not slow"
```

## Notes

Implemented locally on 2026-05-14.

- Added `src/interface/api/{__init__,__main__,routes,runtime,server}.py`.
- Added `vector-design-api` console script.
- Added `tests/interface/api/test_api_t1102.py` covering route-registry parity with the CLI, dependency-free OpenAPI route metadata, injectable public handlers, admin-service client routing, missing admin-client errors, validation WebSocket stream events, optional FastAPI app construction, and the static direct-import boundary.
- Verification passed locally: focused T-1102 slice, adjacent Phase 11/admin-service smoke tests, full ruff, full strict mypy, agenda consistency, and full non-slow pytest (559 passed / 2 skipped).
