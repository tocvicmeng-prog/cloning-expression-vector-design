# T-602 Task Brief — Biology-Back-End-Dependent Predicates

## Module

- ID: `engine.validation.predicates`
- Phase: 6
- Model tier: Opus
- Estimated context budget: <= 55 k tokens.

## Inputs

- `CODING_AGENDA.md` § 2.6.2.
- `task_artefacts/T-601a..k/post_execution.md`.
- T-502 `ValidationContext` and deterministic validation semantics.
- T-405 rule manifests and fixture inventory.

## Deliverables

- Pure predicates for MR-12, MR-13, MR-14, MR-15, MR-16, MR-27, and MR-28.
- Predicate modules consume precomputed metric payloads only; no adapter imports from engine code.
- `IMPLEMENTED_PREDICATE_REGISTRY` extended while the Phase-4 `PREDICATE_REGISTRY` stub registry remains unchanged for manifest consistency.
- Tests proving predicate behavior against T-601 metric payload shapes and sequence fallbacks.

## Acceptance

```yaml
task_id: T-602
phase: 6
owning_modules:
  - engine.validation.predicates
required_checks:
  - "python -m uv run --no-editable pytest tests\\engine\\validation\\test_biology_predicates_t602.py --cov=engine.validation.predicates.rbs --cov=engine.validation.predicates.kozak --cov=engine.validation.predicates.uorf --cov=engine.validation.predicates.premature_polya --cov=engine.validation.predicates.splice --cov=engine.validation.predicates.cpg --cov=engine.validation.predicates.signal_peptide --cov=engine.validation.predicates.metric_helpers --cov-report=term-missing --cov-fail-under=85"
  - "python -m uv run --no-editable python tools\\ci\\run_pytest.py -m \"not slow\""
acceptance:
  - id: A1
    description: "Biology predicates are present in the implemented registry without mutating the Phase-4 stub registry."
    test: "tests/engine/validation/test_biology_predicates_t602.py::test_t602_registry_adds_biology_predicates_without_mutating_stub_registry"
    blocks_verification: true
  - id: A2
    description: "MR-12 through MR-16 consume precomputed adapter metrics or deterministic sequence fallbacks."
    test: "tests/engine/validation/test_biology_predicates_t602.py"
    blocks_verification: true
  - id: A3
    description: "MR-27 and MR-28 evaluate CpG and signal-peptide/compartment consistency."
    test: "tests/engine/validation/test_biology_predicates_t602.py"
    blocks_verification: true
ci_gates_touched: []
```

## Notes

BR-14 remains excluded. T-602 predicates are metric consumers; T-603 owns orchestration and cache binding for computing those metrics from adapters.
