# T-901 Task Brief — EMBL + GFF3 adapters

## Scope

Implement `adapter.io` EMBL and GFF3 sequence I/O adapters on top of the T-308
`ImportedConstruct` / `WriteResult` contract. EMBL should use Biopython's EMBL
reader/writer path and preserve feature annotations through the shared Biopython
conversion helpers. GFF3 should round-trip feature annotations with an embedded
FASTA sequence section; annotation-only GFF3 is rejected because this project's
reader contract requires a concrete `SequenceRecord`.

## Acceptance

```yaml
task_id: T-901
required_checks:
  - python -m uv sync --frozen --no-editable --group dev --extra io --reinstall-package cloning-expression-vector-design
  - python -m uv run --no-editable ruff check src/adapter/io/embl.py src/adapter/io/gff3.py src/adapter/io/_biopython.py src/adapter/io/__init__.py tests/adapter/io/test_sequence_io.py
  - python -m uv run --no-editable mypy src/adapter/io/embl.py src/adapter/io/gff3.py src/adapter/io/_biopython.py tests/adapter/io/test_sequence_io.py
  - python -m uv run --no-editable pytest tests/adapter/io/test_sequence_io.py -q
```

## Notes

Generated by `tools/initial_task_brief_generator.py`.
