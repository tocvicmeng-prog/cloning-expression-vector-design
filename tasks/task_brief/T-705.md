# T-705 Task Brief — `app.assembly_orchestrator` (iterative loop)

## Scope

Replace the T-203 `app.assembly_orchestrator` placeholder with the Phase 7 fixed-point loop.

Implemented files:

- `src/app/assembly_orchestrator.py`
- `tests/app/test_assembly_orchestrator_t705.py`

## Acceptance

```yaml
task_id: T-705
required_checks:
  - python -m uv run --no-editable ruff check src/app/assembly_orchestrator.py tests/app/test_assembly_orchestrator_t705.py
  - python -m uv run --no-editable mypy src/app/assembly_orchestrator.py tests/app/test_assembly_orchestrator_t705.py
  - python -m uv run --no-editable pytest tests/app/test_assembly_orchestrator_t705.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
```

## Notes

Complete locally on 2026-05-14.

- Adds typed orchestration request/result models, iteration history, `AssemblyMethodPicker`, and `ConvergenceFailure`.
- Runs codon optimisation, optional validation, assembly planning, primer design, optional post-primer validation, and fixed-point re-optimisation.
- Uses a five-iteration cap and stable fingerprints over coding sequence, assembly plan, primer sequences, validation findings, and primer warnings.
- Integrates T-701 `CodonOptimiser`, T-703 `AssemblyEngine`, T-704 `PrimerDesigner`, and optional T-603 validation runner hooks.
