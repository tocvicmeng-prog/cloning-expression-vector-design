# T-313a Task Brief — `AuditAppendPort` + `AdminAuditAppendPort` Protocols + `ServicePrincipal` + fake brokers (v1.4 — early-half split per B4-03; scheduled before T-308 / T-310 / T-311 so they can depend on it)

## Scope

Implement the early audit-append split from `CODING_AGENDA.md` § 2.3.9:

- define `domain.ports.audit_append.AuditAppendPort` and `AdminAuditAppendPort`;
- define the append-only `AuditEntry` / `AuditEntryId` value surface;
- define `domain.security.service_principals.ServicePrincipal`;
- provide deterministic in-memory fake engine/admin brokers sharing one HMAC chain;
- add reusable chain-integrity helpers that detect tampered messages and broken links.

## Acceptance

```yaml
task_id: T-313a
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/ports/test_audit_append_contract.py tests/fakes/security/audit_append/test_fake_brokers.py tests/security/test_audit_append_chain_integrity_helpers.py tests/ports/test_port_inventory.py --cov=domain.ports --cov=domain.security.service_principals --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "AuditAppendPort and AdminAuditAppendPort expose append only"
      - "ServicePrincipal is the engine-process caller identity"
      - "Fake engine/admin brokers append to one linear HMAC chain via AuditKeyProvider.mac()"
      - "Chain-integrity helper detects tampered row messages and broken previous-HMAC links"
      - "Port inventory still matches the 50 canonical ports"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-313a/post_execution.md`.
