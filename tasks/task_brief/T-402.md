# T-402 Task Brief - Parts catalogue population

## Scope

Populate `catalogues/parts.yaml` from `Cloning_and_Expression_Vector_Design_Knowledge_Base_v2_0.md` sections 5.1 through 5.13:

- origins of replication;
- selectable markers;
- bacterial and eukaryotic promoters;
- RBS, Kozak, terminator, and polyadenylation entries;
- tags, linkers, protease cleavage sites, and localisation signals;
- reporters, recombinase/integration systems, CRISPR/RNA-guided cassette parts, and inducible mammalian systems.

Each entry must carry a stable id, Sequence Ontology role id, role label, host compatibility, licence/provenance fields, maintenance metadata, and a graded citation resolving to KB v2.0 section 18.

## Acceptance

```yaml
task_id: T-402
required_checks:
  - name: focused_parts_catalogue_tests
    command: "python -m uv run --no-editable pytest tests/adapter/catalogue tests/ci_gates/test_t401_catalogue_gates.py --cov=adapter.catalogue --cov=tools.ci_gates.stale_catalogue_check --cov=tools.ci_gates.source_grade_citation_check --cov-report=term-missing --cov-fail-under=85"
    expected: "10 passed; coverage >= 85%"
  - name: agenda_and_manifest_smoke
    command: "python tools/agenda_consistency_check.py && python -m uv run --no-editable pytest tests/test_t203_module_manifest.py tests/ci_gates/test_t204_gates.py"
    expected: "all pass"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "289 passed, 2 skipped"
```

## Post-Execution Notes

Completed locally on 2026-05-14. T-403 is next.
