# T-301 Task Brief — `domain.sequence` primitives

## Scope

Implement the sequence-domain primitives from `CODING_AGENDA.md` § 2.3.1 and
`ARCHITECTURE.md` § 4.6:

- alphabet and molecule-type enums;
- immutable `Sequence` subclasses for DNA, RNA, protein, and oligo bodies;
- `SequenceRecord` with stable canonical checksum handling for circular records;
- location algebra with fuzzy bounds, between-base locations, circular wrap, and compound locations;
- structured qualifiers and ordered features;
- typed construct hash container values.

## Acceptance

```yaml
task_id: T-301
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/sequence --cov=domain.sequence --cov-fail-under=95"
    covers:
      - "domain.sequence package coverage is at least 95 percent"
      - "circular checksum invariance under rotation"
      - "location algebra invariants"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green after replacing the T-203 stub"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "domain import boundaries remain intact"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-301/post_execution.md`.
