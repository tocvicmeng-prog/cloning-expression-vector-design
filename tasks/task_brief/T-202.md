# T-202 Task Brief — Directory Scaffold

## Scope

Create the project directory tree and package markers required by `CODING_AGENDA.md` § 2.2.2 and `ARCHITECTURE.md` § 4.8.

## Acceptance

```yaml
task_id: T-202
required_checks:
  - tests/test_t202_scaffold.py
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
```

## Notes

Catalogue YAML files are placeholders only. T-401/T-406 own schema validation and real catalogue content.
