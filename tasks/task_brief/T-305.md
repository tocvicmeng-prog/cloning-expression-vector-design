# T-305 Task Brief — `domain.events` (typed events, three streams; v1.2 — H2-03 ownership cleanup)

## Scope

Implement the typed event hierarchy from `CODING_AGENDA.md` § 2.3.5:

- `DomainEvent` base with deterministic serialization;
- stream-specific `DesignEvent`, `GovernanceEvent`, and `ExportEvent` subclasses;
- design-stream `ScreeningCompleted`, `OperationalProtocolAuthorised`, and `SopRendered`;
- governance-stream admin, reviewer, advisory, SOP-template, audit-key, and authentication events;
- export-stream bundle/redaction events.

## Acceptance

```yaml
task_id: T-305
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/events --cov=domain.events --cov-fail-under=85"
    covers:
      - "event serialization and deserialization round-trips"
      - "ScreeningCompleted / OperationalProtocolAuthorised / SopRendered live on design stream"
      - "governance events embed full canonical payloads plus hashes"
      - "active advisory presentation event fields are present"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green after replacing the T-203 event stub"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "domain import boundaries remain intact"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-305/post_execution.md`.
