# T-408 Task Brief — Markers schema v1.0 (new file; v0.2)

## Scope

Seed brief generated from `docs/task_manifest.yaml`.
`/dev-orchestrator` refines this before assignment.

## Acceptance

```yaml
task_id: T-408
required_checks: []
```

## Notes

Generated by `tools/initial_task_brief_generator.py`.

## v0.2.1 audit-fix-M1 note — binding spec location

This file is an **intentional seed stub** produced by
`tools/initial_task_brief_generator.py` to satisfy the
`test-task-brief-coverage` CI gate.

The **binding G1 protocol-completeness specification** for this card
(12-point check: inputs, outputs, algorithm, ≥3 test cases, performance budget,
acceptance criteria, exit conditions, dependencies, tier, owning module,
deliverables, fixtures) **lives in `CODING_AGENDA.md` Section 2**, under the
phase + sub-section that owns this card. For v0.2 cards (T-407..T-415, T-408a,
T-1401..T-1411) the relevant sections are:

- Phase 4.2 cards: `CODING_AGENDA.md § 2.4.2.*`
- Phase 14 cards: `CODING_AGENDA.md § 2.14.*`

The 2026-05-23 dev-orchestrator audit (§ 2 Issue #2) flagged that the
auto-generated stubs leave the G1 gate evaluable only against this dispersed
composite of CODING_AGENDA + docs/task_manifest.yaml + ARCHITECTURE.md, rather
than against a self-contained brief. The v0.2.1 audit decision (recorded in
the collaborative-audit synthesis § 3 + user acceptance 2026-05-23) was to
**document the deferred-by-design status** rather than back-fill 21 full
briefs retroactively. Future cards (v0.3 cadence and beyond) will ship with
full G1 briefs from the start.

For implementation status / handoff documentation, see the per-card
`Implementation status (2026-05-23)` block embedded inside `CODING_AGENDA.md`
itself — these blocks were authored as the cards landed and contain the
actually-executed deliverable shape, dependency resolutions, and follow-on
backlog for each card.
