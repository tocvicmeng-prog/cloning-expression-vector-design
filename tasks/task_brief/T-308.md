# T-308 Task Brief — `adapter.io.GenBank` + `FASTA` + `SBOL3` + `SnapGeneDnaReader`

## Scope

Implement the sequence I/O adapter boundary from `CODING_AGENDA.md` § 2.3.12:

- GenBank read/write through Biopython with `FeatureV14` and location conversion;
- FASTA sequence-only read/write with explicit annotation-loss warnings;
- SBOL 3 sequence read/write with deterministic sorted N-Triples output;
- read-only SnapGene `.dna` parsing through `snapgene-reader` with actionable failure handling;
- imported/annotated construct value contracts shared by downstream adapter and session work.

## Acceptance

```yaml
task_id: T-308
required_checks:
  - command: "python -m uv run --no-editable pytest tests/adapter/io/test_sequence_io.py --cov=adapter.io --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "GenBank feature and qualifier round trip"
      - "FASTA sequence round trip and annotation-loss warning"
      - "SBOL sequence round trip and feature-loss warning"
      - "SnapGene corrupted-file fallback error"
      - "ImportedConstruct and AnnotatedConstruct invariants"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-308/post_execution.md`.
