# T-703 Task Brief — `engine.assembly` (strategy hierarchy; v1.1 — added T-703e SLIC per M-02)

## Scope

Convert the T-203 `engine.assembly` placeholder into a pure assembly strategy package.

Implemented files:

- `src/engine/assembly/__init__.py`
- `src/engine/assembly/base.py`
- `src/engine/assembly/restriction_ligation.py`
- `src/engine/assembly/gibson.py`
- `src/engine/assembly/golden_gate.py`
- `src/engine/assembly/gateway.py`
- `src/engine/assembly/lic.py`
- `src/engine/assembly/slic.py`
- `src/engine/assembly/user.py`
- `src/engine/assembly/iva.py`
- `src/engine/assembly/yeast_tar.py`
- `tests/engine/assembly/test_strategies_t703.py`

## Acceptance

```yaml
task_id: T-703
required_checks:
  - python -m uv run --no-editable ruff check src/engine/assembly tests/engine/assembly/test_strategies_t703.py
  - python -m uv run --no-editable mypy src/engine/assembly tests/engine/assembly/test_strategies_t703.py
  - python -m uv run --no-editable pytest tests/engine/assembly/test_strategies_t703.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
```

## Notes

Complete locally on 2026-05-14.

- Adds shared `AssemblyStrategy` contract with `validate_parts`, `design_primers`, and `compile_assembly_plan`.
- Adds `AssemblyEngine` registry facade for downstream app orchestration.
- Emits existing typed domain plan subclasses for every planned chemistry family.
- Golden Gate strategies delegate overhang-set selection and scoring to T-702 `engine.overhang`.
- SLIC is modelled as a Gibson-like sibling with a dedicated `SLICPlan` carrying T4-polymerase chew-back conditions.
