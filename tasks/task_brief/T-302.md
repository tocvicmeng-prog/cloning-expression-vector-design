# T-302 Task Brief — `domain.graph` (ConstructGraph)

## Scope

Implement the canonical construct graph from `CODING_AGENDA.md` § 2.3.2 and
`ARCHITECTURE.md` § 4.6 / M4:

- immutable `GraphNode`, `EdgeKind`, `Edge`, and `ConstructGraph` primitives;
- endpoint and topology validation against the owning `SequenceRecord`;
- deterministic canonical JSON round-trip;
- `derive_feature_table(graph)` as the canonical-to-derived feature-table rule.

## Acceptance

```yaml
task_id: T-302
required_checks:
  - command: "python -m uv run --no-editable pytest tests/domain/graph --cov=domain.graph --cov-fail-under=95"
    covers:
      - "ConstructGraph canonical JSON round-trip"
      - "edge endpoint validation"
      - "topology consistency with SequenceRecord"
      - "feature_table == derive_feature_table(graph)"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green after replacing the T-203 graph stub"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "domain import boundaries remain intact"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-302/post_execution.md`.
