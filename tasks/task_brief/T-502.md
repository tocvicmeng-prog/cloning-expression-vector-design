# T-502 Task Brief — Pure validation DAG executor

## Module

- ID: `engine.validation`
- Phase: 5
- Model tier: Opus
- Estimated context budget: ≤ 60 k tokens

## Inputs

- `CODING_AGENDA.md` § 2.5.2
- `engine.dependencies.DependencyGraph` from T-501
- `domain.types.validation_rule.ValidationRule`
- T-405 predicate registry and manifest stubs

## Deliverables

- Picklable `ValidationContext` carrying immutable-by-copy design payload, precomputed metrics, threshold profile, derivation-environment hash, and deterministic per-rule seed derivation.
- `ValidationReport`, `RuleEvaluation`, and `RuleEvaluationError` payloads with canonical JSON and content hashing.
- Pure `validate(...)` executor that walks the T-501 dependency graph in producer-before-consumer order, supports field/metric incremental revalidation, routes HARD failures to declared `SafetyGate` blocks, and aggregates predicate errors without aborting the batch.
- `WorkerPoolFactory` with sequential, thread, and local-spawn process modes; library imports must not call `multiprocessing.set_start_method`.
- Pickle-safe registry stubs so every T-405 predicate can be transported under spawn semantics.
- T-502 benchmark file and first local result record under `tests/benchmark/results/`.
- Enforced `no-domain-impurity-check` static gate for domain/engine boundary purity.

## Acceptance

```yaml
task_id: T-502
phase: 5
owning_modules:
  - engine.validation
  - tools.ci_gates.no_domain_impurity_check
required_checks:
  - "python -m uv run --no-editable pytest tests\\engine\\validation tests\\benchmark\\T_502_validation_bench.py -m \"not slow\" --cov=engine.validation --cov=engine.dependencies --cov-report=term-missing --cov-fail-under=85"
  - "python -m uv run --no-editable python -m tools.ci_gates.no_domain_impurity_check --enforce"
gates_activated:
  - BlockCompile routing from HARD validation findings
gates_advanced_to:
  - no-domain-impurity-check: enforced
acceptance:
  - id: A1
    description: "Validation walks rules in deterministic dependency order."
    test: "tests/engine/validation/test_validation_engine.py::test_validate_walks_rules_in_dependency_order"
    blocks_verification: true
  - id: A2
    description: "HARD, SOFT, and INFO severities route findings and blocked gates correctly."
    test: "tests/engine/validation/test_validation_engine.py::test_validate_routes_hard_soft_and_info_severity"
    blocks_verification: true
  - id: A3
    description: "Changed fields and changed metrics evaluate only affected transitive rules."
    test: "tests/engine/validation/test_validation_engine.py::test_validate_incrementally_evaluates_affected_rules_only"
    blocks_verification: true
  - id: A4
    description: "Missing predicates, runtime exceptions, missing metrics, and bad return types are aggregated as typed errors."
    test: "tests/engine/validation/test_validation_engine.py::test_validate_aggregates_missing_predicate_and_runtime_errors"
    blocks_verification: true
  - id: A5
    description: "Reports are byte-deterministic across repeated runs."
    test: "tests/engine/validation/test_validation_engine.py::test_validation_report_canonical_json_is_stable_over_repeated_runs"
    blocks_verification: true
  - id: A6
    description: "Registered predicates and validation contexts are pickle-safe."
    test: "tests/engine/validation/test_validation_engine.py::test_registered_predicates_are_picklable"
    blocks_verification: true
  - id: A7
    description: "Worker pools preserve submit order and process mode uses a local spawn context."
    test: "tests/engine/validation/test_worker_pool_factory.py"
    blocks_verification: true
  - id: A8
    description: "Benchmark file and local result record exist for nightly execution."
    test: "tests/benchmark/T_502_validation_bench.py"
    blocks_verification: true
ci_gates_touched:
  - no-domain-impurity-check
```

## Downstream

- T-504 can reuse `ValidationContext`, `ValidationReport`, and gate-routing semantics while implementing host compatibility reports.
- T-503 can replace selected registry stubs with real structural predicates without changing scheduler semantics.
- T-603 can call `validate(..., changed_fields=..., changed_metrics=...)` after computing affected adapter metrics.
