# T-1101 Task Brief — Typer-based CLI (v1.4 — admin commands route via `AdminServiceClientPort` per B4-08; no direct `AdminActionHandler` import)

## Scope

Implement the Phase 11 CLI as the first user-facing command surface.

- Replace the `interface.cli` placeholder with a package containing command metadata, an injectable runtime, and a process entry point.
- Expose the public commands from `CODING_AGENDA.md` § 2.11.2.
- Route profile, SOP-template, audit-key, and review-queue admin commands through `AdminServiceClientPort`; CLI code must not import the admin action handler.
- Support Typer when the optional `cli` extra is installed, while preserving a stdlib fallback runner for the base/dev test environment.

## Acceptance

```yaml
task_id: T-1101
required_checks:
  - python -m ruff check src/interface/cli tests/interface/cli/test_cli_t1101.py
  - python -m mypy src/interface/cli tests/interface/cli/test_cli_t1101.py
  - python -m pytest tests/interface/cli/test_cli_t1101.py
  - python -m pytest tests/test_t202_scaffold.py tests/ci_gates/test_t204_gates.py tests/ports/test_port_inventory.py tests/domain/ports/test_admin_service_client_contract.py tests/fakes/admin_service/test_in_memory_client.py tests/interface/cli/test_cli_t1101.py
  - python tools/agenda_consistency_check.py
  - python tools/ci/run_pytest.py -m "not slow"
```

## Notes

Implemented locally on 2026-05-14.

- Added `src/interface/cli/{__init__,__main__,commands,entrypoint,runtime}.py`.
- Added `vector-design` and `cev-design` console scripts.
- Added `tests/interface/cli/test_cli_t1101.py` covering registry completeness, injectable public handlers, admin-service client routing, fallback CLI JSON output, missing admin-client errors, and the static direct-import boundary.
- Verification passed locally: focused T-1101 slice, adjacent contract/gate smoke tests, full ruff, full strict mypy, agenda consistency, and full non-slow pytest (550 passed / 2 skipped).
