# T-1001 Task Brief — Vendor adapters (Twist / IDT / GenScript; v1.3 — activates vendor-profile-feasibility portion of `BlockVendorSubmission` per H3-02)

## Scope

Implement static synthesis-vendor adapters for Twist, IDT, and GenScript from `catalogues/vendor_profiles/*.yaml`; expose the T-1001 `SynthesisVendorAdapter` feasibility, partitioning, and cost-estimation surface; and activate the vendor-profile-feasibility portion of `BlockVendorSubmission` without making `engine` import adapter code.

## Acceptance

```yaml
task_id: T-1001
required_checks:
  - python -m uv run --no-editable ruff check src\adapter\vendor src\engine\vendor_feasibility_gate.py tests\adapter\vendor tests\engine\test_vendor_feasibility_gate_t1001.py
  - python -m uv run --no-editable ruff format --check src\adapter\vendor src\engine\vendor_feasibility_gate.py tests\adapter\vendor tests\engine\test_vendor_feasibility_gate_t1001.py
  - python -m uv run --no-editable mypy src\adapter\vendor src\engine\vendor_feasibility_gate.py tests\adapter\vendor tests\engine\test_vendor_feasibility_gate_t1001.py
  - python -m uv run --no-editable pytest tests\adapter\vendor tests\engine\test_vendor_feasibility_gate_t1001.py -q
  - python -m uv run --no-editable lint-imports --config .importlinter
  - python -m uv run --no-editable python tools\ci\run_pytest.py -m "not slow"
```

## Notes

Completed locally on 2026-05-14. Post-execution notes live in `task_artefacts/T-1001/post_execution.md`.
