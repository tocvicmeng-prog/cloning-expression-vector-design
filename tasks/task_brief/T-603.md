# T-603 Task Brief — `app.validation_orchestrator` (metric pre-compute)

## Module

- ID: `app.validation_orchestrator`
- Phase: 6
- Model tier: Opus
- Estimated context budget: <= 45 k tokens.

## Inputs

- `CODING_AGENDA.md` § 2.6.3.
- T-501 `DependencyGraph` affected-rule and required-metric APIs.
- T-502 pure `engine.validation.validate` and `ValidationContext`.
- T-601a..k deterministic biology adapter port shapes.
- T-602 metric-backed biology predicates.

## Deliverables

- Replace the T-203 placeholder with a real application orchestrator.
- Determine affected rules and required metrics from the dependency graph plus predicate metric bindings.
- Compute biology metrics through injected adapter ports, outside the pure engine layer.
- Cache metrics per design session, derivation-environment hash, and source fingerprint.
- Invoke the pure validator with a populated `ValidationContext`.
- Tests for incremental revalidation, derivation-environment cache binding, and missing-adapter failures.

## Acceptance

```yaml
task_id: T-603
phase: 6
owning_modules:
  - app.validation_orchestrator
required_checks:
  - "python -m uv run --no-editable pytest tests\\app\\test_validation_orchestrator_t603.py -q"
  - "python -m uv run --no-editable python tools\\ci\\run_pytest.py -m \"not slow\""
acceptance:
  - id: A1
    description: "Incremental re-evaluation after one module change re-runs only affected rules and metrics."
    test: "tests/app/test_validation_orchestrator_t603.py::test_incremental_module_change_recomputes_only_affected_rules_and_metrics"
    blocks_verification: true
  - id: A2
    description: "Metric cache reuse is bound to the derivation-environment hash."
    test: "tests/app/test_validation_orchestrator_t603.py::test_metric_cache_is_bound_to_derivation_environment_hash"
    blocks_verification: true
  - id: A3
    description: "Missing required biology adapters fail before pure validation is invoked."
    test: "tests/app/test_validation_orchestrator_t603.py::test_missing_required_biology_adapter_fails_before_pure_validation"
    blocks_verification: true
ci_gates_touched: []
```

## Notes

The orchestrator composes `PREDICATE_REGISTRY` with `IMPLEMENTED_PREDICATE_REGISTRY` so implemented T-503/T-602 predicates override stubs while unimplemented manifest rules remain informational until later promotion.
