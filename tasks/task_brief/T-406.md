# T-406 Task Brief - Vendor profiles + screening trust policy + institutional policy + export profiles + risk advisories

## Scope

Populate the Phase 4 policy/profile catalogues from `CODING_AGENDA.md` section 2.4.6, KB v2.0 sections 11, 12, 13, and 17, plus the architecture v1.5 administrator-controlled advisory and unsupported-tier defaults.

The catalogue set must include:

- vendor profiles for Twist, IDT, and GenScript;
- screening trust policy for IGSC v3, IBBIS Common Mechanism, SecureDNA, and institutional blacklist adapters;
- institutional policy defaults for administrator-controlled authorisation, asymmetric administrator/reviewer roles, dual-control defaults, unsupported BSL-4 behavior, and active advisory acknowledgement;
- export profiles for internal audit, vendor submission, public redacted sharing, and regulatory/IBC review;
- risk advisories tied to active acknowledgement and safety-gate semantics.

## Acceptance

```yaml
task_id: T-406
required_checks:
  - name: focused_policy_catalogue_tests
    command: "python -m uv run --no-editable pytest tests/adapter/catalogue tests/ci_gates/test_t401_catalogue_gates.py tests/ci_gates/test_t405_rule_gates.py --cov=adapter.catalogue --cov=tools.ci_gates.stale_catalogue_check --cov=tools.ci_gates.source_grade_citation_check --cov=tools.ci_gates.rule_fixture_coverage_check --cov=tools.ci_gates.implementation_status_consistency_check --cov=engine.validation --cov-report=term-missing --cov-fail-under=85"
    expected: "36 passed; coverage >= 85%"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "315 passed, 2 skipped"
```

## Post-Execution Notes

Completed locally on 2026-05-14. T-316c is next.
