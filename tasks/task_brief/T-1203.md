# T-1203 Task Brief — `adapter.snapgene.SnapGeneApiClient` (UR-01b SHOULD)

## Scope

Complete locally. Implement `adapter.snapgene.SnapGeneApiClient` for the conditional UR-01b live SnapGene channel, using an injectable SnapGene Server Request API transport and falling back to the UR-01a file-watch channel whenever the live API is unavailable or unconfigured.

## Acceptance

```yaml
task_id: T-1203
required_checks:
  - python -m pytest tests/adapter/snapgene/test_api_client_t1203.py -q
  - python -m pytest tests/adapter/snapgene -q
  - python -m ruff check .
  - python -m mypy src tests
  - python -m pytest -m "not slow" -q
```

## Notes

- Current SnapGene support documentation states SnapGene has no public third-party API; the legacy SnapGene Server Request API is documented for existing server users only.
- `SnapGeneApiClient` defaults to unavailable unless a request transport is configured, and routes fallback work through `SnapGeneFileWatcher`.
- Full local verification passed on 2026-05-14 with 599 Python tests passed and 2 skipped.
