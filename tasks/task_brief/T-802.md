# T-802 Task Brief — `engine.design_plan` (v1.2 — renderer files explicit per M2-04)

## Scope

Implement the Phase 8a always-renderable design-plan generator and deterministic renderers for `engine.design_plan`.

- Generate `DesignRealisationPlan` values from an upstream `AssemblyPlanSummary`, primer set, biosafety classification, validation hashes, and optional `RiskAdvisoryReport`.
- Populate assembly-route, fragment-input, QC checkpoint, expected verification artefact, institutional approval, and reviewer-packet summary/evidence fields without operational protocol types.
- Render the plan as canonical JSON, deterministic Markdown, and deterministic PDF bytes.
- Add static coverage proving `engine.design_plan` does not import `domain.types.sop_protected` or reference `ProtocolStep`.

## Acceptance

```yaml
task_id: T-802
implemented_files:
  - src/engine/design_plan/__init__.py
  - src/engine/design_plan/generator.py
  - src/engine/design_plan/renderers/json.py
  - src/engine/design_plan/renderers/markdown.py
  - src/engine/design_plan/renderers/pdf.py
  - tests/engine/test_design_plan_t802.py
required_checks:
  - python -m uv run --no-editable ruff check src/engine/design_plan tests/engine/test_design_plan_t802.py
  - python -m uv run --no-editable mypy src/engine/design_plan tests/engine/test_design_plan_t802.py
  - python -m uv run --no-editable pytest tests/engine/test_design_plan_t802.py -q
  - python tools/agenda_consistency_check.py
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
verification_result:
  focused: 3 passed
  full: 450 passed, 2 skipped
```

## Notes

Refined and completed during T-802 execution.
