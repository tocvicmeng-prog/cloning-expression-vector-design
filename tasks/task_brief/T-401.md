# T-401 Task Brief - Catalogue loader framework + JSON Schemas

## Scope

Implement the Phase-4 catalogue loading foundation from `CODING_AGENDA.md` section 2.4.1:

- convert `adapter.catalogue` into the canonical catalogue package;
- provide a generic YAML document loader with maintenance metadata parsing;
- register one JSON Schema per catalogue family;
- validate all seed YAML catalogues with deterministic errors;
- parse graded citations for source-quality checks;
- activate the stale-catalogue and source-grade citation gates as informational checks;
- document the handoff contract for T-402 through T-406 and T-316b.

## Acceptance

```yaml
task_id: T-401
required_checks:
  - name: focused_catalogue_tests
    command: "python -m uv run --no-editable pytest tests/adapter/catalogue tests/ci_gates/test_t401_catalogue_gates.py --cov=adapter.catalogue --cov=tools.ci_gates.stale_catalogue_check --cov=tools.ci_gates.source_grade_citation_check --cov-report=term-missing --cov-fail-under=85"
    expected: "7 passed; coverage >= 85%"
  - name: agenda_and_manifest_smoke
    command: "python tools/agenda_consistency_check.py && python -m uv run --no-editable pytest tests/test_t203_module_manifest.py tests/ci_gates/test_t204_gates.py"
    expected: "all pass"
  - name: full_static_gates
    command: "python -m uv run --no-editable ruff format --check . && python -m uv run --no-editable ruff check . && python -m uv run --no-editable mypy src tools tests && python -m uv run --no-editable lint-imports --config .importlinter"
    expected: "all pass"
  - name: full_pytest
    command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    expected: "286 passed, 2 skipped"
```

## Post-Execution Notes

Completed locally on 2026-05-14. T-402 is next.
