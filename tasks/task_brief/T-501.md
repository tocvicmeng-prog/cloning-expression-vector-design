# T-501 Task Brief — Validation dependency DAG

## Module

- ID: `engine.dependencies`
- Phase: 5
- Model tier: Opus
- Estimated context budget: ≤ 50 k tokens

## Inputs

- `CODING_AGENDA.md` § 2.5.1
- `domain.types.validation_rule.ValidationRule`
- Rule manifests populated in T-405

## Deliverables

- Deterministic dependency graph over validation rules, read field paths, produced metrics, required metrics, and invalidation declarations.
- Affected-rule computation from changed fields or changed metrics.
- Required metric and invalidated metric/rule reporting for T-502 validation execution.
- Producer-before-consumer topological rule order with explicit cycle detection.
- Diagnostics exposing typed graph nodes and edges.

## Acceptance

```yaml
task_id: T-501
phase: 5
owning_modules:
  - engine.dependencies
required_checks:
  - "python -m uv run --no-editable pytest tests\\engine\\test_dependencies.py --cov=engine.dependencies --cov-report=term-missing --cov-fail-under=90"
gates_activated: []
gates_advanced_to: []
acceptance:
  - id: A1
    description: "Field changes propagate from field readers through produced metrics to downstream metric consumers."
    test: "tests/engine/test_dependencies.py::test_dependency_graph_marks_field_readers_and_metric_consumers_affected"
    blocks_verification: true
  - id: A2
    description: "Metric changes trigger only dependent consumers and their downstream invalidations."
    test: "tests/engine/test_dependencies.py::test_dependency_graph_supports_metric_first_revalidation"
    blocks_verification: true
  - id: A3
    description: "Explicit invalidates entries mark rules and metrics stale."
    test: "tests/engine/test_dependencies.py::test_dependency_graph_tracks_explicit_invalidated_rules_and_metrics"
    blocks_verification: true
  - id: A4
    description: "Producer cycles are rejected with a deterministic error."
    test: "tests/engine/test_dependencies.py::test_dependency_graph_topological_order_rejects_cycles"
    blocks_verification: true
  - id: A5
    description: "Graph nodes and edges are exposed for diagnostics."
    test: "tests/engine/test_dependencies.py::test_dependency_graph_exposes_nodes_and_edges_for_diagnostics"
    blocks_verification: true
ci_gates_touched: []
```

## Downstream

- T-502 consumes `DependencyGraph` to schedule pure validation-rule execution.
- T-603 can use `DependencyImpact.required_metrics` to request only affected adapter metrics.
