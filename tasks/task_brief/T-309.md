# T-309 Task Brief — `engine.session` state machine + replay determinism + gate-predicate versioning

## Scope

Implement the Phase-3 session skeleton from `CODING_AGENDA.md` § 2.3.13:

- event-sourced `DesignSession` state machine;
- deterministic replay from design events, with governance cross-stream invariants;
- snapshot selection keyed by `DerivationEnvironment.hash()`;
- pending safety-gate predicate registry for all four `Block*` gates;
- predicate version/content-hash tracking for deterministic replay;
- safety-gate activation map documentation.

## Acceptance

```yaml
task_id: T-309
required_checks:
  - command: "python -m uv run --no-editable pytest tests/engine/session tests/domain/events/test_events.py --cov=engine.session --cov=domain.events --cov-report=term-missing --cov-fail-under=85"
    covers:
      - "state transitions and terminal blocked states"
      - "100 synthetic replay-determinism sessions"
      - "cross-stream governance payload invariant"
      - "pending Block* gate registry"
      - "predicate version-history selection"
      - "snapshot lookup by derivation-environment hash"
  - command: "python -m uv run --no-editable python tools/ci/run_pytest.py -m \"not slow\""
    covers:
      - "repository unit suite remains green"
  - command: "python -m uv run --no-editable lint-imports --config .importlinter"
    covers:
      - "architecture import contracts remain green"
  - command: "python -m uv run --no-editable mypy src tools tests"
    covers:
      - "strict typing remains green"
  - command: "python -m uv run --no-editable ruff check ."
    covers:
      - "lint remains green"
```

## Notes

Completed locally on 2026-05-14. See `task_artefacts/T-309/post_execution.md`.
