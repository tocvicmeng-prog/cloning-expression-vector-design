# T-902 Task Brief — `adapter.snapgene.SnapGeneFileWatcher` (v1.2 — H2-04 fix; depends on T-308e for `.dna` parsing)

## Scope

Implement `adapter.snapgene.SnapGeneFileWatcher` as the UR-01a file-watch
interchange channel. The watcher observes SnapGene-exported GenBank files,
debounces burst writes, imports through `GenBankAdapter`, runs an injected
validation hook, and emits paired GenBank output for SnapGene re-import.
Optional `.dna` watch support must call the T-308e `SnapGeneDnaReader` re-exported
from `adapter.snapgene`; this task must not add a local `dna_reader.py`.

## Acceptance

```yaml
task_id: T-902
required_checks:
  - python -m uv sync --frozen --no-editable --group dev --extra io --reinstall-package cloning-expression-vector-design
  - python -m uv run --no-editable ruff check src/adapter/snapgene tests/adapter/snapgene
  - python -m uv run --no-editable ruff format --check src/adapter/snapgene tests/adapter/snapgene
  - python -m uv run --no-editable mypy src/adapter/snapgene tests/adapter/snapgene
  - python -m uv run --no-editable pytest tests/adapter/snapgene -q
```

## Notes

Generated by `tools/initial_task_brief_generator.py`.
