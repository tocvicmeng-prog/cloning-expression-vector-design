# T-806b Task Brief — Authorisation gate decision + review-queue routing + adversarial UAT (v1.3 — routes blocked sessions to T-315 review queue per B3-06)

## Scope

Implemented in `src/app/authorisation_decision.py` and `src/engine/operational_protocol_gate.py`.

The T-806b slice now evaluates post-screening operational authorisation from real
`ScreeningCompleted` events, user declarations, verified signed authorisation profiles,
and T-806a advisory acknowledgement governance events. Denied attempts emit
`AuthorisationAttemptDenied` and route blocked sessions into the T-315 review queue.

## Acceptance

```yaml
task_id: T-806b
required_checks:
  - python -m uv run --no-editable ruff check .
  - python -m uv run --no-editable mypy src tools tests
  - python -m tools.ci_gates.no_passive_advisory_bypass_check --enforce
  - python -m uv run --no-editable python tools/ci/run_pytest.py -m "not slow"
```

## Notes

Complete locally on 2026-05-14. Focused T-806b slice: 15 tests passed. Full local
non-slow pytest after `uv sync --reinstall-package cloning-expression-vector-design`:
530 passed, 2 skipped.
