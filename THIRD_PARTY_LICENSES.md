# Third-Party Licenses

This file starts the T-201 license ledger. T-204 will replace this stub with a generated transitive dependency report.

## Runtime Dependencies

| Package | Purpose | License source |
|---|---|---|
| `cryptography` | signatures and audit-key primitives | package metadata |
| `pydantic` | boundary parsing | package metadata |
| `pyyaml` | YAML catalogue parsing | package metadata |

## Optional Dependency Groups

Optional extras are recorded in `pyproject.toml` and `uv.lock`. Their transitive licenses are reviewed before each extra moves from scaffold to production use.

## Bundled Assets

| Asset | Purpose | License |
|---|---|---|
| `fonts/Noto-Sans-Regular.ttf` | canonical PDF/UI rendering font | SIL Open Font License 1.1 |
| `fonts/Noto-Mono-Regular.ttf` | canonical monospace rendering font | SIL Open Font License 1.1 |

See `fonts/LICENSE-NOTO.md` for the bundled font license notice.
